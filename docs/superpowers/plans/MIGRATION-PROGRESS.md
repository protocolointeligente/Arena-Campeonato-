# Arena — Legacy → src/ Migration Progress

Durable tracker for the incremental "strangler fig" migration of
`arena-campeonatos-v2-intervencao-19.html` (still shipped to production via
`scripts/prepare-legacy.mjs`) into the modular `src/` Vite app. Source of
truth for what's actually landed on `main` — conversation memory does not
survive compaction, this file does.

Mode: autonomous. User has authorized executing every phase to completion,
always taking the recommended path, without stopping to ask — see decision
log below. Only stop for a genuine blocker.

## How each phase runs

1. Write/refresh the phase's plan under `docs/superpowers/plans/` (writing-plans skill).
2. `EnterWorktree`, then immediately `git merge main --ff-only` (worktrees branch from `origin/main`, not local `main` — see the decision log entry below).
3. Execute via `superpowers:subagent-driven-development`: implementer → task review → (fix loop) per task, ledger at `.superpowers/sdd/progress.md` inside the worktree. Every new/changed `class="..."` gets checked against `layout.css`/`tokens.css` for both the class's own rule and any `<class> <child-selector>` rule — see "Process note" below.
4. Final whole-branch review (opus) → fix Critical/Important → re-review.
5. `npm test && npm run build && npm run verify` green on the merged result.
6. Merge to `main` locally (recommended default, established Phase 1), delete branch, clean up worktree.
7. Update this file.

## Decision log

- 2026-08-15: User chose incremental migration (strangler fig) over finishing the rewrite blind or abandoning it — condition: every ported function must actually work, not stub.
- 2026-08-15: User authorized autonomous execution of all remaining phases, always the recommended option, no further check-ins until the project is done.
- 2026-08-15: Found `npm test` on `main` (not the Phase 1 worktree) picking up stray Jest specs from an untracked `.chrome-cdp/` directory at the repo root (an unrelated leftover browser-extension checkout). Fixed with `vitest.config.js` scoping `include` to `src/**/*.test.js` — commit `d684c28`. Unrelated to the migration itself but blocked honestly verifying "tests green" on `main`, so fixed inline rather than deferred.
- 2026-08-15: A fresh worktree created with `EnterWorktree` branches from `origin/<default-branch>` by default, not local `main` — since Phase 1 was merged locally but never pushed, Phase 2a's worktree initially missed it entirely. Fixed by `git merge main --ff-only` inside the worktree right after creation. **Standing instruction for every future phase:** immediately after `EnterWorktree`, run `git merge main --ff-only` (or push local `main` to `origin` first) before doing anything else — don't rediscover this the hard way each time.

## Known follow-ups (not bugs, deliberately deferred — surfaced by Phase 2a's final review)

- **Public portal, PDF export, and registration-approval only see the *active* category.** Root-mirroring (`state.teams`/`state.matches`) means `public-championship.js`, `services/pdf.js`, and the registration-approval handler in `championship.js` all read/write whichever category was active at last save — switch categories and save anything, and the public link's team/match list silently changes to the new active category (the full data isn't lost — it's all still in `state.categories` — just not what these three surfaces currently read). Not urgent: `src/` isn't the live production surface yet (`scripts/prepare-legacy.mjs` still ships the legacy monolith). Must be resolved before Phase 7 (build cutover) — either by making these three surfaces category-aware, or by an explicit product decision that they intentionally show "whichever category was last active."
- **Registration approvals have no way to pick which category a new team joins** — same root cause as above.
- Cosmetic, low-priority: `addCategory`'s default name (`Categoria ${length+1}`) can repeat after a remove-then-add cycle (ids stay unique, so no functional bug); `ordem` field is written but nothing reads it yet (dormant until Phase 2d's phase ordering needs it); category audit log entries don't name which category was added/renamed/removed.
- Pre-existing, predates Phase 2a, not introduced by it: `[data-pdf]`/`[data-publication]` in `championship.js`'s `bind()` use `addEventListener` on nodes that live outside the re-rendered `content` section and are never cleaned up, so listeners accumulate one per `render()` call. `[data-remove-team]` filters `state.teams` without fixing up the numeric `match.home`/`match.away` indices in `state.matches`, corrupting any already-generated fixture list when a team is removed after fixtures exist. Worth a small fix-it pass (own plan, not blocking) before Phase 3b (Matches & scoring) builds more on top of the match/team relationship.

## Known follow-ups — Phase 2b (Teams & Roster)

- **Image payload strategy needs revisiting before Phase 5.** `compressPhoto` stores photos as base64 data URIs directly in the Firestore document (a faithful legacy port, not a new risk on its own) — but combined with category root-mirroring, each photo is now duplicated across `state.teams` and `state.categories[].teams`, and the whole state re-serializes on every save. A few dozen photographed athletes can approach Firestore's 1 MiB per-document limit. Phase 5 (branding/sponsors) adds more images via `readAndResizeImage` — worth switching to Firebase Storage + URL references before that lands, rather than compounding the same pattern a second time.
- `roster.js`'s `athleteById`/`athName`/`teamNameById` are ported and tested but have no caller yet in `src/` — they're the read-side helpers Phase 3 (match events, disciplinary views) will need. Not dead code, just early.
- `class="btn ghost sm"` (`championship.js`, roster modal) has no `.sm` CSS rule anywhere in `src/styles/` — pre-existing gap (already used elsewhere before this phase), same class of bug `.row` was in Task 2 before its fix. Worth a sweep for every `class="..."` in `src/pages/*` with no matching selector, rather than fixing them one at a time as each is noticed.
- `persist()` (in `championship.js`) now catches a failed `saveChampionship` and toasts it, but callers still proceed to write an `addAudit(...)` entry for a change that was never actually saved (a log-accuracy gap, not data loss — the in-memory state is intact for a retry). Fix belongs with a broader look at `persist()`'s callers, not a one-line patch.
- `categoriesView()`'s category row (2-3 children) now sits in the same 4-column `.team-row` grid the roster UI needed; the unused 4th track shifts its "Remover" button ~10px from where it sat before. Cosmetic only.
- `updateAthlete`'s `dob`/`numero` fields accept only strings (`(dob || '').trim()`-style coercion) — fine for every current DOM-sourced caller, but worth a guard if a future non-DOM caller (e.g. a bulk-import feature) ever passes a number or Date.

## Known follow-ups — Phase 2c (Venues, Officials & Team Staff)

- `venueById`/`officialById` are ported and tested but uncalled until Phase 3b wires them into match scheduling — same "early, not dead" status as Phase 2b's read helpers.
- `officials[].phone` is supported by `ops.js` and not yet populated by any UI, so no leak today — but when Phase 3b's match-ops screen adds a phone field, `publicState()` (`services/championships.js`) needs to strip it before it reaches `publicChampionships`, the same way `foto` was stripped in Phase 2b.

## Known follow-ups — Phase 2d (Phases & format)

- **Mid-implementation integration bug, caught and fixed within the phase (not shipped broken):** Task 3 correctly made `saveRootIntoActive` sync a phase's `formato`/`cfg` from root state, which exposed 4 `engine.test.js` fixtures (Task 2) that mutated `phase.formato` directly without updating the matching root field — `saveRootIntoActive`'s now-correct sync silently clobbered those test setups. Fixed same-session in `7c5bc8e` (test-fixture-only fix, no production code changed) before Task 3's review closed. Lesson for 3a/3c, who will keep touching this same root/phase seam: `generateActivePhase` now carries a comment at its `saveRootIntoActive(state)` call explaining the invariant.
- **Whole-branch review caught two scope gaps the per-task reviews couldn't see** (both fixed before merge, commit `11d8688`): `new-championship.js`'s creation wizard still offered a `mata` format option that led to a dead end once selected (no matching Fases-tab option, `generateActivePhase` refuses it) — removed. `phase.cfg.nGrupos`/`turnos` had no editor anywhere, hardwiring every phase to 2 groups/single-turno — added number inputs to the phase row in `fasesView()`.
- `addCategory` (`categories.js`) inline-builds a blank-phase object literal that duplicates the field set `phases.js`'s internal (unexported) `blankPhase()` helper already produces for `addPhase`. Real DRY gap now that both call sites exist — exporting `blankPhase` and reusing it would be a low-risk one-line fix. Deferred, not blocking.
- `progressionSummary(category, phase)` has no null-guard on `phase` (unlike `saveRootIntoPhase`, which does). Safe today — the only call site (`fasesView()`) always passes a real phase — but worth a guard if a future caller passes a possibly-undefined one.
- Progression config can show a UI/data mismatch: if a phase's format changes from `grupos` to `liga` after `progression.mode` was set to `'perGroup'`, the mode `<select>` falls back to displaying "Melhores no geral" (the `perGroup` option disappears when format ≠ grupos) while the summary text beside it still reads "por grupo" from the stored data. `setPhaseFormat` doesn't reset `progression.mode` on a format change. Cosmetic; Phase 3c (which builds the actual classification logic) is the natural place to decide the right behavior here.
- `standings()` (`championship.js`) merges all matches into one flat table regardless of `match.grupo` — correct for `liga`/pre-2d, silently wrong for a `grupos`/`gxg` phase (multiple groups shown as one table). Real per-group standings is Phase 3c's job (`computeStandings`/`standingsTable`); this phase makes multi-group phases *reachable* without making them *correctly displayed* yet.
- The championship header's format badge (`state.formato.toUpperCase()`) is written once in `mount()` and never refreshed after a phase switch or format change — goes stale. Pre-existing pattern (header built once, not re-rendered), not introduced by this phase, just newly visible now that format can change without a full page reload.
- `venueById`/`officialById` (2c) and `roster.js`'s read-helpers (2b) remain uncalled — still waiting on Phase 3b (match scheduling/events).

## Known follow-ups — Phase 3a (Draw & Bracket Engine)

- **Fixed a latent legacy bug, not a regression:** legacy's `phaseComplete`/`qualifiedFromPhase` called a function named `winnerOf` that was never defined anywhere in the 12,000-line monolith (confirmed by exhaustive grep) — a dead `ReferenceError` path if either branch had ever been reached in production. This phase defines `winnerOf` properly in `engine.js`; `phases.js`'s `phaseComplete` inlines the equivalent one-line check instead of importing it, to avoid a circular dependency (`engine.js` already imports from `phases.js`).
- **Two real correctness bugs were found and fixed during this phase's own review loop** (not shipped broken — surfaced before merge): (1) `resolveTie`/`advanceBracket` initially conflated a genuine construction-time bye with a mid-tournament pending slot, crowning premature winners; the fix's first pass then regressed non-power-of-two brackets (5,6,9-14 teams) into being permanently unfinishable, caught by the final whole-branch review and fixed with structural bye detection, then a second review round found the fix needed 2+ `advanceBracket` calls to converge a multi-round bye cascade (n=9/10), fixed by restructuring into a single topological pass. (2) A pre-existing bug from Phase 2d — `[data-phase-turnos]`/`[data-phase-ngrupos]` handlers mutating `phase.cfg` directly, silently clobbered by `persist()`'s root→phase sync whenever editing the *active* phase — was caught here (while fixing the same-shaped bug in this phase's new mao-unica/terceiro toggles) and fixed for all four handlers. See the plan's task-review history for full traces.
- **No regression test locks in single-call bracket-advancement convergence** — the sweep test (`engine.test.js`, "every team count from 2 to 16 can reach a champion") loops `advanceBracket` until quiescence, so it would stay green even if the single-pass fix were reverted to the old two-phase relaxation. Cheap to add (snapshot before/after a second call, assert no change) — not done here, flagged by the final review as test debt rather than a live defect.
- **Already-persisted brackets for n ∈ {5,6,9,10,11,12} created before this phase's fixes may have a permanently-stuck third-place tie** (one side `null` forever, from before the `ids.length > size * 3/4` gate existed). Regenerating the phase (`Gerar/Refazer`) clears it — not a live-data migration concern since `src/` isn't the production surface yet, same status as every other Phase 2/3a follow-up.
- The "3º lugar" checkbox in Fases stays checked/enabled even for team counts where no third-place match can structurally exist (the setting silently has no effect at generation time for those counts). Cosmetic.
- `standings()` (Tabela tab) shows an all-zero table for a `mata` phase — it reads `state.matches`, which mata phases never populate (all data lives in `state.bracket`). `games()`/`config()` grew a one-line redirect note for `mata`; `standings()` doesn't have an equivalent yet. Same class of gap Phase 2d already left for `grupos`/`gxg` (no per-group standings) — real fix is Phase 3c's `computeStandings`/`standingsTable`.
- A championship created via the wizard with `formato:'mata'` gets no `cfg.maoUnica` set, so its first phase defaults to two-legged ties (four score boxes) rather than single-leg — faithful to legacy's own default, but worth a UX look (default `maoUnica:true` in the wizard) if two-legged surprises organizers.
- `renderMataFromGroups` and `qualifiedFromPhase`'s `mata` branch remain deferred to Phase 3c (need `computeStandings` for the groups-phase classification half of that pipeline, same reasoning as Phase 2d's `applyProgression` deferral) — now buildable correctly once 3c lands, since `winnerOf` and a real `bracket.rounds` shape exist.

## Known follow-ups — Phase 3b (Matches & scoring)

- **Fixed a latent legacy bug, not a regression:** `splitInfo`'s dead fallback line (a self-contradicting expression that only ever evaluated to `''` or `undefined`) was simplified to `parts.slice(2).join(' · ') || ''`.
- **Whole-branch review found 4 Important + 2 Minor issues, all fixed before merge** (fix commit `e8c8cbe`, re-reviewed): (1) the `[data-score]` instant-save handler called `render()` right after `persist()`, destroying the score grid mid-use — a real data-loss bug, `render()` removed. (2) `saveMatchOps` never cleared `meta.venueText` (the one-time legacy free-text migration field), so explicitly clearing a match's venue in the ops modal left the stale legacy text showing via `metaLine`'s fallback — fixed by clearing `venueText` alongside `venueId`. (3) legacy BR-formatted dates (`dd/mm/yyyy`) migrating from `match.info` were assigned raw into `meta.date`, corrupting `<input type="date">`; added `toISODate()` to convert (and safely no-op on garbage) before assignment. (4) the two new `addAudit` calls for score/match-ops updates omitted `before`/`after`, breaking the audit trail's data — both now pass `result.before`/`result.after`. Minor: dropped double-`esc()`-escaping on audit summary team names (convention is raw-store, escape-at-render); `clearResults` now calls `advanceBracket` after resetting bracket ties so a persisted bracket doesn't sit stale until the next Chaveamento render.
- **Re-review found the venueText fix's own regression test was weak** (round-tripped through a freshly-generated single-segment `match.info` that never populated `venueText` in the first place, so it passed with or without the fix) — rewritten (`e66adf6`) to seed a real `·`-delimited legacy `info` string so `matchMeta`'s migration genuinely backfills `venueText` before the clear is asserted; verified by hand that the strengthened test fails when the fix line is reverted.
- `clearResults` doesn't reset `meta.status` (a match cleared of its score still shows `status: 'live'`/`'finished'` if the ops modal had set one) — cosmetic, no data-loss, still open (Phase 4a addressed the sibling `.events` gap noted below, not this one).
- ~~`clearResults` empties `match.scorers` but Phase 4's disciplinary/events model may introduce a richer `events` field on matches; when that lands, `clearResults` should be revisited to reset whatever superset field replaces `scorers`.~~ **Done in Phase 4a** — `clearResults` now resets `.events` alongside `.scorers` on both matches and bracket ties.
- Legacy free-text `venueText`/malformed-date edge cases (info strings that don't match the `dd/mm/yyyy · hh:mm · local` shape at all) degrade gracefully (empty fields) rather than erroring, but aren't exhaustively tested beyond the one garbage-date case — acceptable given `src/` isn't the production surface yet.

## Known follow-ups — Phase 3c (Standings & scorers)

- **Whole-branch review found a real correctness bug, fixed before merge** (commit `4140624`): `genCross` and the `grupos`/`gxg` branches of `standings()` mapped team ids to array indices via `findIndex` with no guard — a stale id in `state.grupos` or a match's `grupo` referencing a team no longer in `state.teams` resolved to index `-1` and corrupted the computation. Both call sites now filter `-1` results.
- `cardRanking` is ported and tested but has no UI caller yet — its only caller is Phase 3d's `viewDisciplina`, same "early, not dead" status Phase 2b/2c's read-helpers had.
- `standsToRows` is ported and tested but uncalled — its only caller is Phase 4b's PDF export (`reportStandingsBlocks`).
- ~~`scorerRanking`/`cardRanking` read `match.events`, empty until Phase 4's súmula UI populates it — the Artilharia tab will show its empty state until then, expected.~~ **Done in Phase 4a** — the Súmula modal now writes `match.events`/tie `.events`; Artilharia shows real scorers once goals are recorded.

## Known follow-ups — Phase 3d (Discipline & Suspensions)

- **Final whole-branch review found 2 Important issues, fixed before merge**: (1) the criteria reorder/remove/add handlers called `render()` without first capturing the sibling win/draw/loss-points and yellow-limit fields in the same card, silently discarding an unsaved edit — fixed by extracting a shared `captureScoring()` helper called at the top of all four scoring-card handlers. (2) `critMove`'s boundary-no-op test compared the result against the same array object passed in, so it couldn't detect a mutating regression against the plan's own "must not mutate" constraint — fixed with a literal-array assertion plus an explicit input-unchanged check.
- **`cfg` (win/draw/loss points, tie-break criteria, disciplinary weights, yellow-card suspension limit) is scoped to the *active phase*, not the whole championship** — same root cause as every other `state.cfg`-backed setting in `src/`, not new to this phase. `yellowLimit` in particular reads as a competition-wide rule in the UI; switching to a different pre-existing phase silently restores that phase's own last-saved value. The config card now carries a one-line "vale para a fase ativa" label rather than silently implying championship-wide scope. Real fix (making discipline config championship-wide, or explicitly per-phase everywhere) is a data-model decision, not a review-blocking bug — worth deciding alongside Phase 7's cutover.
- **Discipline is active-phase-scoped, not tournament-scoped**: `cardRanking`/`suspensionInfo` both read `allMatchObjs(state)`, which only sees the active phase's matches/bracket (inherited from Phase 3c, not introduced here). A player suspended in the group stage shows clean once a knockout phase becomes active — "N yellows across the whole tournament" is the normal real-world rule. Still open post-4a (`match.events` is now real, but no cross-phase aggregation exists) — needs a cross-phase match aggregation if/when this becomes a real complaint.
- **Name-only card events can never be flagged as suspended**: `cardRanking` keys on `athleteId || name` (`src/app/standings.js`), while the suspended-players banner only matches on `athlete.id` (iterating each team's roster). Phase 4a's Súmula "s/ atleta" (anonymous) buttons confirmed this is now a live, reachable case, not hypothetical — an anonymous card appears in the Cartões table but never triggers the suspension warning, and every anonymous event for a team collapses into one aggregated row (they all key as `n:|<teamId>`). Still open; the fuller fix is prompting for a name on the anonymous buttons.
- ~~`clearResults` (`src/app/matches.js`, Phase 3b) does not clear `match.events`~~ **Done in Phase 4a** — `clearResults` now resets `.events` on both matches and bracket ties.
- `yellowLimit: 0` is accepted by the input (`min="0"`) but every reader falls back to 3 via `|| 3`, so 0 is indistinguishable from "unset" rather than meaning "no suspensions" — cosmetic, `min="1"` or a `?? 3` fallback would resolve it if anyone hits it.
- The ↑/↓ criteria-reorder buttons get a `disabled` attribute at the list boundaries, but `.btn` has no `[disabled]` CSS rule anywhere in `src/styles/`, so a disabled button doesn't look disabled — same class of gap as `.sm` having no rule (tracked since Phase 2b), worth a combined CSS sweep rather than a one-off fix.

## Known follow-ups — Phase 4a (Súmula & Match Events)

- **Final whole-branch review found 2 Important issues, fixed before merge** (fix commit `5286ab9`): (1) `removeMatchEvent`'s bounds guard (`index < 0 || index >= length`) was `false` for `NaN`/non-integer input, so a bad index fell through to `splice`, which coerces it — silently removing the wrong event while still returning `{ok:true}`. Fixed by adding a `Number.isInteger(index)` check, plus a regression test. (2) The three Súmula handlers (add/anon/remove) mutated match/tie events and persisted, but never called `addAudit`, unlike every other mutation in `championship.js` (score updates, athlete/venue/official add-remove, etc.) — a red card that can suspend a player left no trace in Histórico. Fixed by adding `addAudit(state.id, 'match_event_added'/'match_event_removed', <summary>)` to all three handlers.
- A mid-task review also caught and fixed a `<hr class="sep">` divider in the Súmula modal — `.sep` had no matching CSS rule anywhere, violating the "no new selectors" constraint; replaced with an inline `border-top:1px solid var(--line)` div (commit `c76166c`).
- `removeMatchEvent` is positional (`events[index]`), even though every event already carries a `uid()` `.id` that nothing reads. A double-click on the ✕ button before the modal's `await persist()` resolves and re-renders could remove a different event than the one clicked. Removing by `.id` instead would close this race. Not fixed here (plan-conformant as written) — worth doing whenever the Súmula modal is next touched.
- Anonymous card/goal events (the "s/ atleta" buttons) always record `name: ''` with no prompt, faithfully matching legacy — see the Phase 3d follow-up above for the resulting aggregation/suspension gaps this causes.
- The "Súmula" button on a match row (`games()`) renders unconditionally, even when `home`/`away` haven't resolved to real teams yet ("A definir") — opens a modal with an empty header and no team pickers, a dead end rather than a crash. `tieRow()`'s equivalent button is correctly gated on `canScore`; `games()`'s isn't. Cosmetic, worth matching the same gating next time this code is touched.

## Known follow-ups — Phase 5a (Branding & Sponsors)

- **Image payload strategy still partially open.** `compressPhoto` (Phase 2b, athlete/team photos) still stores base64 data URIs in Firestore — the risk flagged in Phase 2b's follow-ups is not closed. This phase switched only the *new* branding/sponsor images to Firebase Storage URLs. Migrating the existing athlete/team photo fields requires backfilling already-persisted documents and is a separate, larger task deferred past Phase 7.
- **No delete-on-replace/remove for orphaned Storage objects** — deliberate `ponytail:` deferral. Old logo/cover/sponsor-logo files accumulate in Storage when replaced or removed. Add `deleteObject()` cleanup in `src/services/storage.js` if Storage costs ever matter.
- **No platform-admin override in `storage.rules`** — deliberate `ponytail:` deferral. A superadmin cannot edit another organizer's branding images via the Storage path without first adding a `firestore.get(platformAdmins/{uid})` check to `storage.rules`.
- `publicState()` in `src/services/championships.js` does **not** strip `state.branding`/`state.sponsors` — these are intentionally public fields (the public portal renders them). Confirmed correct; no change needed.

## Known follow-ups — Phase 6a (Collaborators & Roles)

- **Firestore rules make the role system non-functional for collaborators and unenforced server-side — pre-existing, faithful legacy parity, not introduced by this phase, but this is the phase that ships the UI to create collaborators, so it's the phase where the gap stops being theoretical.** (1) `saveChampionship` (`src/services/championships.js`) batch-writes both `championships/{id}` and its `publicChampionships/{id}` mirror; the public write's rule requires `request.resource.data.ownerUid == request.auth.uid`, which is never true for an invited collaborator — Firestore batches are atomic, so the whole save is rolled back and every collaborator-initiated action fails with "Erro ao salvar". Legacy has the identical dual-write against the identical owner-only rule (`arena-campeonatos-v2-intervencao-19.html:412-413`/`:538`) — legacy's own management card even carried an inline warning about exactly this ("Nesta versão o acesso é vinculado ao e-mail. Para produção, as regras do Firestore devem validar ownerUid e collaboratorEmails no servidor.") that the port dropped. (2) `firestore.rules`'s `canManageChampionship()` grants any collaborator role — including `viewer` — unconstrained write on the whole championship document, with no invariant pinning `ownerUid`/`collaboratorEmails` unchanged; `myRole`/`can`'s client-side gate is the only thing enforcing role boundaries today, so a viewer with direct Firestore access could rewrite ownership. **Both must be resolved before Phase 7 cutover** — `src/` isn't the production surface yet (`scripts/prepare-legacy.mjs` still ships the legacy monolith), so this doesn't block Phase 6a's merge, but it blocks collaborators actually working and blocks any real security boundary.
- `isOwner` (`src/app/collaborators.js`) intentionally has no superadmin override — it's a synchronous pure function and `isSuperadmin()` (`src/services/superadmin.js`) is async. The override lives one layer up: `src/pages/championship.js`'s `mount()` resolves `isSuperadmin()` once into a closure-scoped `superadmin` flag (defaults `false`, fails closed on error/no-auth) and ORs it into both the `persist()` and `managementView()` gates. No re-render is triggered when the flag resolves — a superadmin who acts within the single Firestore round-trip before it lands sees a spurious denial; near-unhittable in practice (one doc read vs. a human click) and fails closed, so left as-is rather than adding a `render()` call on resolution.
- The "Seu perfil: X" role badge in the championship header is a bare `roleLabel()` string (`<span class="tag">Proprietário</span>`) rather than legacy's `Seu perfil: ${roleLabel()}` — sitting next to the "Salvo" tag it's slightly ambiguous chrome. Also, for a non-owner superadmin the badge reads "Sem acesso" (computed from `myRole`, which knows nothing about the page-layer `superadmin` override) while saves succeed — a real but cosmetic contradiction created by combining the superadmin gate with the role badge in the same fix round.
- `inviteManager` (`src/app/collaborators.js`) still allows an `admin`-role collaborator (not just the owner) to invite/re-invite, including silently upgrading an existing collaborator's role — this is dead code from the UI's perspective today, since `managementView()`'s invite form is now owner/superadmin-only (`editable` gate), exactly mirroring legacy's identical dead branch. Left as-is for legacy parity; would need a matching UI change if admin-initiated invites are ever meant to be reachable.
- Audit log entries for collaborator actions (`collaborator_invited`/`_role_changed`/`_removed`) are written unconditionally after `await persist()`, which swallows its own save errors — so a failed save (e.g. the Firestore-rules gap above) still produces an audit entry claiming the grant happened. Same pre-existing pattern as every other handler in this file, flagged as a Phase 2b follow-up already ("`persist()` ... callers still proceed to write an `addAudit(...)` entry for a change that was never actually saved") — not new to this phase, not fixed here.
- `auth.currentUser` is read synchronously at various points in `championship.js` (the new role badge included) on the assumption that Firebase's auth-state restore has already resolved by the time the page mounts — an existing assumption this file already made everywhere else, not introduced by this phase, but worth a Phase 7 note alongside the Firestore-rules items above.
- Phase 6's remaining scope (superadmin platform panel beyond the existing billing-approval stub, audit/security/privacy/beta centers, plans & billing) is not yet planned into 6b/6c/6d — this phase covered only the collaborators/roles slice.
- **Merged against a concurrently-landed Phase 4a** (main had advanced to `8ea2a3f`/`5d95c96` — súmula/match-events and the Phase 4→4a/4b split — while this phase's worktree was in flight). Conflict was confined to `src/pages/championship.js`'s import list and the single dense `bind()` line; resolved by keeping both phases' handlers (`[data-sumula]` alongside `[data-add-mgr]`/`[data-mgr-role]`/`[data-mgr-remove]`) — see merge commit `768bd33`. Full suite (242 tests, both phases' coverage combined) and build/verify re-run green on the merged result before this entry was written.

## Process note: the class/CSS mismatch bug, three times running

Phase 2b (`.row` used with no CSS rule; `.team-row` overflowing a 5-child athlete row) and Phase 2c (`.team-row` overflowing a 4-child staff row; then the fix's replacement `.row` having no input styling) all shipped the same root cause: markup written against a CSS class without checking what that class's rule — or its children's rule — actually does. Every instance was only caught by the whole-branch review, never the task-level one. **Standing instruction from Phase 2d onward:** for every new or changed `class="..."` in a diff, grep `layout.css`/`tokens.css` for that exact class **and** for `<that-class> <child-selector>` rules (e.g. `.row input`, not just `.row`) before considering the task done — both at implementer self-review and at task-review time, not deferred to the final pass.

## Phase status

| # | Phase | Scope (from the published audit) | Status | Plan | Merge commit |
|---|---|---|---|---|---|
| 1 | Shared foundations | esc/clone/uid, fmtDateBR/brl/ageFrom, toast/modal/loadingHTML | ✅ Done | `2026-08-15-migration-phase1-foundations.md` | `73da3ab` (+ `d684c28` test-scope fix) |
| 2a | Categories | addCategory/removeCategory/renameCategory/ensureCategories/activeCategory/switchCategory/categoryBar (categorySnapshot/categoryTeamLimitReached/canAddTeams deferred to Phase 6, billing-coupled) | ✅ Done | `2026-08-15-migration-phase2a-categories.md` | `6c625a8` |
| 2b | Teams & roster | addAthlete/delAthlete/editAthlete/saveAthlete/athleteById/athName/eaPhoto/compressPhoto/pickLogo/teamById/teamNameById/teamLogoMini (delT→already covered by home.js; readAndResizeImage→moved to Phase 5, branding-coupled; renTeam→existing flow already covers it, seedNames half deferred to Phase 3a) | ✅ Done | `2026-08-15-migration-phase2b-roster.md` | `d433ada` |
| 2c | Venues, officials & team staff | addVenue/delVenue/venueById/addOfficial/delOfficial/officialById/staffRow/setStaff | ✅ Done | `2026-08-15-migration-phase2c-ops.md` | `082f9ea` |
| 2d | Phases & format (liga/grupos/gxg — mata-mata deferred to 3a, applyProgression/qualifiedFromPhase deferred to 3c, see plan) |  (roundRobin/buildFixtures/buildGxg pulled forward from 3a; setFmt/renderFmtOpts belong to the legacy creation wizard, not this flow) | ✅ Done | `2026-08-15-migration-phase2d-phases.md` | `11d8688` |
| 3a | Draw & bracket engine (mata format made real — see plan for rescope) | drawBracket/drawGroups/runDraw/confirmDraw/finishDraw/toggleSeed/refreshSeeds/seedOrder/genCross/makeBracketFromOrdered/buildFixtures/buildGxg/nextPow2/roundRobin/renderDraw/renderBracketView/renderMataFromGroups/advanceBracket/loserOf/findTie (tieObj/resolveTie pulled forward from 3b; drawBracket/drawGroups/runDraw/toggleSeed/refreshSeeds/seedOrder/genCross/renderDraw/confirmDraw/finishDraw/renderMataFromGroups deferred — creation-wizard/3c concerns) | ✅ Done | `2026-08-15-migration-phase3a-draw-bracket.md` | `a9c59a5` |
| 3b | Matches & scoring (instant scoring + match-ops scheduling — see plan for rescope) | setScore/setTie/tieHTML/tieObj/matchRow/matchMeta/matchContext/matchEvents/openMatchOps/saveMatchOps/opsConfigHTML/mark/clearResults/nextMatchesCard/allMatchObjs/h2h/resolveTie (tieObj/resolveTie/setTie/tieHTML already landed in 3a; matchEvents/matchContext/h2h/mark/nextMatchesCard/allMatchObjs deferred — see plan) | ✅ Done | `2026-08-15-migration-phase3b-matches.md` | `e66adf6` |
| 3c | Standings & scorers | computeStandings/standingsForPhase/standingsTable/standsToRows/cardRanking/scorerRanking | ✅ Done | `2026-08-16-migration-phase3c-standings.md` | `4140624` |
| 3d | Discipline & suspensions | viewDisciplina/suspensionInfo/critAdd/critMove/critRemove/critCommit/critTail/criteriaEditor/optCrit/optTurnos | ✅ Done | `2026-08-16-migration-phase3d-discipline.md` | `676fe60` |
| 4a | Súmula & match events | sumulaModal/renderSumula (suObj/suAdd/suAddAnon/suDel pulled forward as plumbing) | ✅ Done | `2026-08-16-migration-phase4a-sumula.md` | `5286ab9` |
| 4b | Reports & exports (PDF) |  | Not started | — | — |
| 5 | Public portal & registration |  + registrationCfg/registrationDeadlineClosed/registrationStatusLabel/setRegistrationDocs/addPublicAthleteRow/copyRegistrationLink + brandingConfigHTML/sponsorsConfigHTML/addSponsor/removeSponsor/ensureBranding/clearBrandImage/setBrandImage/readAndResizeImage | ✅ Done | `2026-08-16-migration-phase5a-branding-sponsors.md` | `01ef54e` |
| 6a | Collaborators & roles | inviteManager/removeManager/changeManagerRole/ensureCollaborators/myCollaborator/myRole/roleLabel/can/isOwner | ✅ Done | `2026-08-16-migration-phase6a-collaborators.md` | `768bd33` |
| 6b+ | Administration (remaining) | renderSuperadmin/loadPlatformAdmin/superOpenChamp + renderAuditCenter/renderSecurityCenter/renderPrivacyCenter/privacyNoticeHTML/renderBetaHardening + choosePlan/currentPlan/planCardsHTML/planLimitText/canCreateChampionship/confirmPlanRequest/approvePendingPlan (suAdd/suAddAnon/suDel/suObj already landed in Phase 4a as súmula plumbing — the original audit's "su" prefix grouped them here, but they're match-event functions, not superadmin) | Not started | — | — |
| 7 | Build cutover | remove `scripts/prepare-legacy.mjs` from the build, expand `scripts/verify-routes.mjs`, canary, retire the monolith | Not started | — | — |

## Resume instructions (post-compaction / new session)

1. Read this file — it's the map.
2. `git log --oneline -15` on `main` to confirm the last merged phase matches the table above.
3. Pick up at the first row still "Not started" / "⏳ Next", following "How each phase runs" above.
