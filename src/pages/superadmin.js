import { navigate } from '../app/router.js';
import { pendingPayments, approvePayment } from '../services/superadmin.js';
export async function renderSuperadmin(root) { root.innerHTML = `<div class="shell"><header class="topbar"><a class="logo" href="/">ARENA</a><button class="btn ghost" data-back>← Voltar</button></header><main class="section"><div class="card">Carregando painel...</div></main></div>`; root.querySelector('[data-back]').onclick = () => navigate('/'); try { const payments = await pendingPayments(); root.querySelector('main').innerHTML = `<small>ADMINISTRAÇÃO</small><h1>Superadmin</h1><p class="muted">Confirme o Pix antes de liberar o plano.</p><div class="card"><h2>Pagamentos pendentes</h2>${payments.length ? payments.map((item) => `<div class="alert-row"><div><strong>${item.email || item.id}</strong><p class="muted">${item.billing?.planId || 'Plano'} · R$ ${Number(item.billing?.amount || 0).toFixed(2).replace('.', ',')}</p></div><button class="btn primary" data-approve="${item.id}">Liberar acesso</button></div>`).join('') : '<p class="muted">Nenhum pagamento pendente.</p>'}</div>`; root.querySelectorAll('[data-approve]').forEach((button) => button.onclick = async () => { await approvePayment(button.dataset.approve); renderSuperadmin(root); }); } catch (error) { root.querySelector('main').innerHTML = `<div class="card"><h2>Acesso restrito</h2><p class="muted">${error.message || 'Você não possui permissão de superadmin.'}</p></div>`; } }
