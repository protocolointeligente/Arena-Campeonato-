import { doc, getDoc } from 'firebase/firestore';
import { db } from '../services/firebase.js';
import { navigate } from '../app/router.js';
function esc(v) { return String(v ?? '').replace(/[&<>"']/g, (c) => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', '"':'&quot;', "'":'&#39;' }[c])); }
export async function renderPublicChampionship(root, id) { root.innerHTML = `<div class="shell"><header class="topbar"><a class="logo" href="/">ARENA</a><button class="btn ghost" data-back>← Voltar</button></header><main class="section"><div class="card">Carregando campeonato...</div></main></div>`; root.querySelector('[data-back]').onclick = () => navigate('/'); const snap = await getDoc(doc(db, 'publicChampionships', id)); if (!snap.exists()) return root.querySelector('main').innerHTML = '<div class="card"><h2>Campeonato não encontrado</h2></div>'; const data = snap.data(); let state = {}; try { state = JSON.parse(data.data || '{}'); } catch {} const standings = (state.teams || []).map((team) => `<li>${esc(team.nome)}</li>`).join(''); root.querySelector('main').innerHTML = `<div class="card"><small>CAMPEONATO</small><h1>${esc(state.nome || data.nome)}</h1><p class="muted">${esc(state.subtitulo || 'Acompanhe os resultados e as equipes.')}</p><div class="grid"><div class="card"><h3>Próximos jogos</h3><p class="muted">${(state.matches || []).filter((m) => m.hg == null || m.ag == null).length} jogos programados.</p></div><div class="card"><h3>Resultados</h3><p class="muted">${(state.matches || []).filter((m) => m.hg != null && m.ag != null).length} jogos encerrados.</p></div><div class="card"><h3>Equipes</h3><ul>${standings || '<li>Nenhuma equipe cadastrada.</li>'}</ul></div></div></div>`; }
