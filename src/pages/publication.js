import { navigate } from '../app/router.js';
export function renderPublication(root, id) { const publicUrl = `${location.origin}/publico/${id}`; const registrationUrl = `${location.origin}/inscrever/${id}`; root.innerHTML = `<div class="shell"><header class="topbar"><a class="logo" href="/">ARENA</a><button class="btn ghost" data-back>← Voltar</button></header><main class="section"><small>PUBLICAÇÃO</small><h1>Compartilhe seu campeonato</h1><div class="grid"><div class="card"><h2>Portal público</h2><p class="muted">Envie este link para a torcida acompanhar resultados e equipes.</p><code class="pix-key">${publicUrl}</code><button class="btn primary" data-copy="${publicUrl}">Copiar link</button><button class="btn" data-open="${publicUrl}">Abrir portal</button></div><div class="card"><h2>Inscrições</h2><p class="muted">Use este link para receber equipes e atletas.</p><code class="pix-key">${registrationUrl}</code><button class="btn primary" data-copy="${registrationUrl}">Copiar link</button><button class="btn" data-open="${registrationUrl}">Abrir formulário</button></div></div></main></div>`; root.querySelector('[data-back]').onclick = () => navigate(`/campeonatos/${id}`); root.querySelectorAll('[data-copy]').forEach((button) => button.onclick = () => navigator.clipboard?.writeText(button.dataset.copy)); root.querySelectorAll('[data-open]').forEach((button) => button.onclick = () => window.open(button.dataset.open, '_blank')); }
