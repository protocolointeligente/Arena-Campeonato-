import { navigate } from '../app/router.js';
import { saveChampionship } from '../services/championships.js';

const id = () => `${Date.now().toString(36)}${Math.random().toString(36).slice(2, 8)}`;
export function renderNewChampionship(root) { root.innerHTML = `<div class="shell"><header class="topbar"><a class="logo" href="/" data-link>ARENA</a><button class="btn ghost" data-back>← Voltar</button></header><main class="section"><form class="card" style="max-width:700px;margin:auto"><small>NOVO CAMPEONATO</small><h1>Vamos começar</h1><label class="muted">Nome<input name="name" required maxlength="100" style="display:block;width:100%;margin:8px 0 16px;padding:12px" placeholder="Ex.: Copa do Bairro 2026"></label><label class="muted">Formato<select name="format" style="display:block;width:100%;margin:8px 0 16px;padding:12px"><option value="liga">Liga</option><option value="grupos">Grupos</option><option value="mata">Mata-mata</option></select></label><p class="muted" data-error></p><button class="btn primary" type="submit">Criar campeonato</button></form></main></div>`; root.querySelector('[data-back]').onclick = () => navigate('/'); root.querySelector('form').onsubmit = async (event) => { event.preventDefault(); const data = new FormData(event.currentTarget); const value = { id: id(), created: Date.now(), updated: Date.now(), nome: data.get('name'), formato: data.get('format'), status: 'rascunho', publico: true, branding: { accent: '#2fcf6b' }, collaborators: [], teams: [], categories: [], matches: [], grupos: [], cfg: { turnos: 1, criterios: ['P', 'V', 'SG', 'GP'] } }; try { await saveChampionship(value); navigate(`/campeonatos/${value.id}`); } catch (error) { root.querySelector('[data-error]').textContent = error.message || 'Não foi possível criar.'; } }; }
